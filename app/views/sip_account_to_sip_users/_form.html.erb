<%= form_for(@sip_account_to_sip_user) do |f| %>
  <% if @sip_account_to_sip_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sip_account_to_sip_user.errors.count, "error") %> prohibited this sip_account_to_sip_user from being saved:</h2>

      <ul>
      <% @sip_account_to_sip_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sip_user_id %><br />
    <%= f.text_field :sip_user_id %>
  </div>
  <div class="field">
    <%= f.label :sip_account_id %><br />
    <%= f.text_field :sip_account_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
