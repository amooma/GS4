<h1><%= t(:editing_manufacturer) %></h1>

<%= render 'form' %>

<%= link_to t(:show), @manufacturer if can?( :show, @manufacturer ) %> |
<%= link_to t(:back), manufacturers_path if can?( :index, Manufacturer ) %>
