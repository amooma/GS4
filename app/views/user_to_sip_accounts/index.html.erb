<h1>Listing user_to_sip_accounts</h1>

<table>
  <tr>
    <th>User</th>
    <th>Sip account</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_to_sip_accounts.each do |user_to_sip_account| %>
  <tr>
    <td><%= user_to_sip_account.user_id %></td>
    <td><%= user_to_sip_account.sip_account_id %></td>
    <td><%= link_to 'Show', user_to_sip_account %></td>
    <td><%= link_to 'Edit', edit_user_to_sip_account_path(user_to_sip_account) %></td>
    <td><%= link_to 'Destroy', user_to_sip_account, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User to sip account', new_user_to_sip_account_path %>
