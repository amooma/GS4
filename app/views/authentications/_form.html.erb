<%= simple_form_for( @authentication ) do |f| %>
	<%= f.error_messages %>
	
	<%= f.association :user, :label_method => :username %>
	<%= f.input :provider %>
	<%= f.input :uid %>
	
	<%= f.button :submit %>
<% end %>
