<%= form_for(@user_to_phone) do |f| %>
  <% if @user_to_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_to_phone.errors.count, "error") %> prohibited this user_to_phone from being saved:</h2>

      <ul>
      <% @user_to_phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :phone_id %><br />
    <%= f.text_field :phone_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
