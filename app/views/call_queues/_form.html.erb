<%= form_for(@call_queue) do |f| %>
  <% if @call_queue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@call_queue.errors.count, "error") %> prohibited this call_queue from being saved:</h2>

      <ul>
      <% @call_queue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :uuid %><br />
    <%= f.text_field :uuid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
