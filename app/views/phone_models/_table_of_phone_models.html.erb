<table>
	<tr>
		<th></th>
		<th><%= t(:name) %></th>
		<th><%= t(:manufacturer) %></th>
		<th><%= t(:number_of_phones) %></th>
		<th><%= t(:max_number_of_sip_accounts) %></th>
		<th><%= t(:number_of_keys) %></th>
		<th></th>
		<th></th>
	</tr>

<% phone_models.each do |phone_model| %>
	<tr class="<%= cycle("even", "odd") %>">
		<td class="vam">
			<%= render :partial => 'phone_models/image', :locals => {:phone_model => phone_model} %>
		</td>
		<td class="vam">
			<%= link_to phone_model.name, phone_model %>
		</td>
		<td class="vam"><%= link_to truncate(phone_model.manufacturer.name, :length => 25), phone_model.manufacturer %></td>
		<td class="vam"><%= phone_model.phones.count %></td>
		<td class="vam"><%= phone_model.max_number_of_sip_accounts %></td>
		<td class="vam"><%= phone_model.number_of_keys %></td>
		<td class="vam"><%= link_to t(:edit), edit_phone_model_path(phone_model) if can?( :edit, phone_model ) %></td>
		<td class="vam"><%= button_to t(:destroy), phone_model, :confirm => 'Are you sure?', :method => :delete if can?( :destroy, phone_model ) %></td>
	</tr>
<% end %>
</table>
