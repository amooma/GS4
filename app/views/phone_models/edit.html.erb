<h1><%= t(:editing_phone_model) %></h1>

<%= render 'form' %>

<%= link_to t(:show), @phone_model if can?( :show, @phone_model ) %> |
<%= link_to t(:Go_to_list), phone_models_path if can?( :index, PhoneModel ) %>
