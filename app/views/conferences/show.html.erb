<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @conference.name %>
</p>

<p>
  <b>Pin:</b>
  <%= @conference.pin %>
</p>

<b>Extensions:</b>
  <% if @conference.extensions.count == 0 %>
    <div class="noticebox">
      You need to create an extension to be able to call the conference room!
  </div>
  <% end %>
  <ul>
  <% @conference.extensions.each do |extension| %>
    <li><%= extension.extension %></li>  
  <% end %>
    <li><%= link_to 'create extension', new_conference_extension_path(@conference, {:destination => @conference.uuid}) %>
  </ul>
</p>

<%= link_to 'Edit', edit_conference_path(@conference) %> |
<%= link_to 'Back', conferences_path %>
