<%= simple_form_for([ @conference, @extension ]) do |f| %>
	<%= f.error_messages %>
	
	<%= f.input :extension %>
	<%= f.input :destination, :input_html => { :readonly => ! @extension.destination.nil? } %>
	<%= f.input :active %>
	
	<%= f.button :submit %>
<% end %>
